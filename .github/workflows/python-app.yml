name: Python application

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version:
          - "3.11"
          - "3.12"
          - "3.13"
          - "3.14"
          - "3.14t"
    services:
      redis:
        image: redis:8
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 10
    env:
      TERM: 'dumb'
    steps:
    - uses: actions/checkout@v6
      with:
        fetch-depth: 0  # Fetch all history for all branches

    - name: Install uv and Python
      uses: astral-sh/setup-uv@v7
      with:
        enable-cache: true
        python-version: ${{ matrix.python-version }}

    - name: Setup uv venv
      run: uv sync --locked --dev

    - name: Lint with ruff
      run: uv run ruff check coredis_utils/ tests/

    - name: Type check with pyright
      run: uv run pyright coredis_utils/ tests/

    - name: Test with pytest
      run: uv run pytest -v tests/
